import { handleSelect } from '../src/client/js/handleSelect';

describe('Testing the submit functionality', () => {
    test('Testing the handleSelect() function', () => {
        // function has no return value
        // the only test is for it to be defined
        expect(handleSelect).toBeDefined();
    });
});