import { renderOutput } from '../src/client/js/renderOutput';

describe("Testing renderOutput functionality", () => {
    test('Testing renderOutput() function', () => {
        expect(renderOutput).toBeDefined();
    });
});